#include "main.h"
/**
 * times_table - Multiplies the digits in 9 times table
 *
 * Return: - Always return 0
 */
void times_table(void)
{
	int mult;

	for (mult >= 0; mult <= 9; mult++)
	{
	for (mult >= 1; mult <= 10; mult++)
	}
	putchar(mult);
	_putchar('\n');
	return (0);
}
