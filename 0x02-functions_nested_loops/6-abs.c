#include "main.h"
/**
 * _abs - computes the absolute integer value
 * @b: -integer to be compiled
 *
 * Return: Always return 0
 */
int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (-b);
}
