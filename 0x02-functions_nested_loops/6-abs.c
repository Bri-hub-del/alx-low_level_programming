#include "main.h"
/**
 * _abs - computes the absolute integer value
 * @x: -integer to be compiled
 *
 * Return: Always return 0
 */
int _abs(int x)
{
	int _abs(int x)
	{
		if (x >= 0)
			return (x);
		else
			return (-x);
	}
}
